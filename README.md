# Hierarchical Battery Metadata Reconstruction

Python implementation for the manuscript:

**Hierarchical battery metadata reconstruction from short-pulse responses for retired lithium-ion batteries**

This repository provides code for reconstructing key battery metadata from short-pulse voltage responses, including material-capacity group, state of charge (SOC), and state of health (SOH). The proposed framework follows a hierarchical reconstruction order from **material-capacity classification** to **SOC estimation** and finally **SOH estimation**.

---

## Important note on data availability

This repository contains the code required to run the proposed framework, benchmark comparisons, ablation studies, measurement-sensitivity analyses, further analyses, and figure-generation scripts.

The battery pulse-response dataset should be placed under:

```text
data/
```

The expected folder structure is described in [`data/README.md`](data/README.md).


---

## Manuscript Information

This repository accompanies the manuscript:

**Hierarchical battery metadata reconstruction from short-pulse responses for retired lithium-ion batteries**

The study develops a short-pulse diagnostic framework for reconstructing retired-battery metadata from voltage-response measurements, including material-capacity group, SOC, and SOH.

---

## Repository Structure

```text
Hierarchical-Battery-Metadata-Reconstruction/
├── ablation/                    # Ablation studies
├── analysis/                    # Further analysis, calibration, and error-propagation analysis
├── benchmark/                   # Benchmark models and fair/enhanced comparisons
├── data/                        # Battery pulse-response data
├── examples/                    # Smoke tests and lightweight examples
├── figures/                     # Manuscript figure-generation scripts
├── measurement_sensitivity/     # Pulse width, C-rate, polarity, missing/noise sensitivity
├── proposed_framework/          # Proposed hierarchical reconstruction framework
├── results/                     # Generated experiment outputs
├── utils/                       # Shared utility functions
├── .gitignore
├── README.md
└── requirements.txt
```

For detailed instructions, see the `README.md` file inside each folder.

---

## Installation

Create a new Python environment:

```bash
conda create -n battery-metadata python=3.11
conda activate battery-metadata
```

Install dependencies:

```bash
pip install -r requirements.txt
```

---

## Minimal Reproducibility Workflow

To reproduce the main computational pipeline:

```bash
# 1. Run the proposed hierarchical reconstruction framework
python proposed_framework/run_proposed_framework.py

# 2. Run further analysis based on proposed-framework outputs
python analysis/run_further_analysis_proposed.py

# 3. Train or evaluate calibration baselines
python analysis/train_calibration_baseline.py

# 4. Run benchmark comparisons
python benchmark/run_all_benchmarks.py

# 5. Generate representative manuscript figures
python figures/plot_fig3a_prediction_scatter.py
python figures/plot_fig5c_calibration.py
python figures/plot_fig5d_fair_unfair_comparison.py
```

Some figure-generation scripts depend on result files generated by earlier experiment or analysis scripts. If a plotting script reports a missing input file, first run the corresponding experiment script and then rerun the plotting script.

---

## Folder-Level Documentation

Detailed instructions are provided in the corresponding folders:

* [`data/README.md`](data/README.md): expected dataset structure
* [`proposed_framework/README.md`](proposed_framework/README.md): proposed framework
* [`benchmark/README.md`](benchmark/README.md): benchmark models and fair/enhanced settings
* [`ablation/README.md`](ablation/README.md): ablation studies
* [`measurement_sensitivity/README.md`](measurement_sensitivity/README.md): pulse-width, C-rate, polarity, missing-feature and noise analyses
* [`analysis/README.md`](analysis/README.md): further analysis, calibration, and error-propagation analysis
* [`figures/README.md`](figures/README.md): figure-to-code mapping
* [`examples/README.md`](examples/README.md): smoke tests and lightweight examples

---

## Output Files

Most scripts save generated outputs under:

```text
results/
```

The `results/` folder contains intermediate files, prediction summaries, benchmark results, ablation outputs, sensitivity-analysis outputs, and files required by figure-generation scripts.

---

## Data Availability

The dataset used in this study should be placed under `data/`. The expected data structure is described in [`data/README.md`](data/README.md).

The public data archive link will be added after data deposition:

```text
[DATASET LINK TO BE ADDED]
```

---

## Code Availability

All scripts required for the proposed framework, benchmark comparisons, ablation studies, measurement-sensitivity analyses, further analyses, and manuscript figure generation are provided in this repository.

---

## Computational Environment

The code was developed and tested with:

* Python 3.11
* PyTorch
* scikit-learn
* XGBoost
* pandas
* NumPy
* matplotlib

A complete package list is provided in `requirements.txt`.

---

## Citation

If you use this code or dataset, please cite:

```text
[CITATION TO BE ADDED AFTER PUBLICATION]
```

---

## License

License information will be added before public release.
